services:
  dashboard:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - codex-usage-data:/data
    environment:
      CODEX_USAGE_DATA_DIR: /data

volumes:
  codex-usage-data:
